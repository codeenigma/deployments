command:
  build:
    develop:
      server:
        www-root: /var/www
        ci-user: jenkins
        host: dev4.int.codeenigma.net
      app:
        location: www
        notifications-email: greg.harvey@codeenigma.com
      hooks:
        pre:
          - hooks/testscript.php